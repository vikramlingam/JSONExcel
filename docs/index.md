# jsonexcel

Convert JSON, JSONL, Python records, and JSON stored inside Excel cells into clean `.xlsx` workbooks.

```python
from jsonexcel import convert

convert("data.json", "data.xlsx")
```

The package is offline-first, deterministic, and safe by default. Start with [Getting started](quickstart.md), then choose the guide for your input and output task.
